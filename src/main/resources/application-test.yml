spring:
  main:
    banner-mode: "off"

security:
  jwt:
    secret: "testSecretKeyWith64BytesMinimumForHS512AlgorithmValidation1234567890"

logging:
  level:
    com.openmeteo.mcp: INFO
    reactor.netty: WARN
    org.springframework: WARN

openmeteo:
  api:
    timeout-seconds: 10  # Shorter timeout for tests
  # Enable chat for integration tests
  chat:
    enabled: true
    memory:
      type: inmemory
