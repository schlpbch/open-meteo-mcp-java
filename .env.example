# ============================================
# Open-Meteo MCP Java Environment Configuration
# ============================================
# Copy this file to .env and fill in your actual values
# This file contains examples of all configurable environment variables

# ============================================
# Application Configuration
# ============================================

# Server port (default: 8888)
# SERVER_PORT=8888

# ============================================
# AI Provider Configuration
# ============================================

# Azure OpenAI (Primary Provider)
AZURE_OPENAI_KEY=your-azure-openai-api-key-here
AZURE_OPENAI_ENDPOINT=https://your-resource-name.openai.azure.com
AZURE_OPENAI_DEPLOYMENT=gpt-4

# OpenAI (Fallback Provider)
OPENAI_API_KEY=your-openai-api-key-here

# Anthropic Claude (Fallback Provider)
ANTHROPIC_API_KEY=your-anthropic-api-key-here

# ============================================
# ChatHandler Configuration
# ============================================

# Enable/disable chat functionality (true/false)
CHAT_ENABLED=false

# Memory type: 'inmemory' or 'redis'
MEMORY_TYPE=inmemory

# ============================================
# Redis Configuration
# ============================================
# Required when MEMORY_TYPE=redis

# Redis connection details
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=your-redis-password-if-any

# Redis database number (default: 0)
# REDIS_DATABASE=0

# ============================================
# Docker Compose Specific Variables
# ============================================
# These are used by docker-compose.yml when running in containers

# Azure OpenAI for Docker Compose (same values as above but used by compose)
# AZURE_OPENAI_API_KEY=your-azure-openai-api-key-here
# AZURE_OPENAI_ENDPOINT=https://your-resource-name.openai.azure.com

# ============================================
# Open-Meteo API Configuration
# ============================================
# These are typically set to default values and rarely need changing

# API endpoints (defaults are usually fine)
# OPENMETEO_WEATHER_URL=https://api.open-meteo.com/v1
# OPENMETEO_AIR_QUALITY_URL=https://air-quality-api.open-meteo.com/v1
# OPENMETEO_GEOCODING_URL=https://geocoding-api.open-meteo.com/v1
# OPENMETEO_MARINE_URL=https://marine-api.open-meteo.com/v1

# API timeout in seconds (default: 30)
# OPENMETEO_TIMEOUT_SECONDS=30

# ============================================
# Development/Testing Variables
# ============================================

# Logging levels (for development)
# LOGGING_LEVEL_ROOT=INFO
# LOGGING_LEVEL_COM_OPENMETEO_MCP=DEBUG

# ============================================
# Notes
# ============================================
#
# Required variables (you must set these):
# - At least one AI provider key (AZURE_OPENAI_KEY, OPENAI_API_KEY, or ANTHROPIC_API_KEY)
# - AZURE_OPENAI_ENDPOINT (if using Azure OpenAI)
# - Redis connection details (if MEMORY_TYPE=redis)
#
# Optional variables have defaults that work for most setups.
#
# For production deployments:
# 1. Set CHAT_ENABLED=true to enable chat functionality
# 2. Set MEMORY_TYPE=redis for persistent conversation memory
# 3. Configure proper Redis instance with authentication
# 4. Use appropriate logging levels